# ══════════════════════════════════════════════════════════════════
#  耄耋改名 · 版本库忽略清单
#
#  原则：**只提交「人手写的」和「能复现交付物的脚本」**。
#  一切能由命令重新生成的东西都不入库 —— 这样仓库小、diff 干净。
# ══════════════════════════════════════════════════════════════════

# ── 依赖（646 MB，用 npm install 一条命令就能还原）──────────────────
node_modules/

# ── 构建产物（npm run build 生成）──────────────────────────────────
out/

# ── 打包产物（npm run package:win 生成，297 MB）────────────────────
release/

# ── 测试覆盖率报告（npm run test:cov 生成）─────────────────────────
coverage/

# ── 冒烟跑出来的截图与报告（electron tools/smoke.js 生成，约 26 MB/次）──
#  报告是「给人看的一次性证据」，不是交付物 —— 要看就地打开即可。
#  需要长期留档时，把那一份 report.html 单独拷出来再提交。
#  注：`**/` 不能省 —— 带中间斜杠的模式会被锚定到本文件所在目录（仓库根），
#  而应用在 maodie-renamer/ 子目录下，只写 tests/artifacts/ 是匹配不到的。
**/tests/artifacts/

# ── TypeScript 增量编译缓存 ────────────────────────────────────────
*.tsbuildinfo

# ── 日志 / 临时文件 ────────────────────────────────────────────────
*.log
npm-debug.log*
tmp-test/
tmp-debug/

# ── 操作系统与编辑器 ──────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/

# ── AI 协作工具的本地工作数据 ─────────────────────────────────────
#  里面是会话日志与本机路径，属于「工作过程」而非「产品交付物」。
#  ⚠ 如果你希望把它当作决策档案一起备份进版本库，
#    把下面这一行注释掉即可（`# .workbuddy/`）。
.workbuddy/
